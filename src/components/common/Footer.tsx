import { Box, Typography } from "@mui/material";

export const Footer = () => {
  return (
    <Box component="footer" py={2}>
      <Typography align="center">
        Footer
      </Typography>
    </Box>
  );
}